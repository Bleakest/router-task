export interface HeroData {
  gender: string;
  image: string;
  id: number;
  name: string;
  species: string;
  status: string;
}

export interface LocationData {
  id: number,
  name: string,
  type: string,
  dimension: string
}

export interface EpisodeData {
  id: number,
  name: string,
  airDate: string,
  episode: string
}